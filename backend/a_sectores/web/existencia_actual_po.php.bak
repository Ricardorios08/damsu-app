<?php 
global $buscador_rapido;

if ($borrar != 1){
$buscador_rapido=$_POST["buscador_rapido"];
}

$hoy = date("d/m/Y");
include("../../../conexiones/config_pro.php");


$B = 1;



$sql1 = "select * from tr_existencias where cod_droga = 0";
$result1 = $db->Execute($sql1);

 if (!$result1) die("fallo".$db->ErrorMsg());
  while (!$result1->EOF) {
  
$cod_mercaderia=$result1->fields["cod_mercaderia"];
$cod_detalle=$result1->fields["cod_detalle"];


$sql="select * from monodrogas where cod_barra = $cod_mercaderia ";
$result = $db->Execute($sql);
$nombre_comercial=strtoupper($result->fields["nombre_comercial"]);
$presentacion=strtoupper($result->fields["presentacion"]);
 $cod_droga=strtoupper($result->fields["cod_droga"]);

 

  $sql = "UPDATE tr_existencias SET cod_droga = '$cod_droga' WHERE cod_detalle = '$cod_detalle'";
$result = $db->Execute($sql);



$cont = $cont + 1;

   $result1->MoveNext();
	}


/*
$a = "inventario_po.xls";

header("Content-type: application/vnd.ms-excel");
header("Content-Disposition: attachment; filename=$a");

*/


 $sql1 = "select * from tr_existencias order by fecha_ultimo_mov desc";
$result1 = $db->Execute($sql1);
$fecha_ultimo_mov=$result1->fields["fecha_ultimo_mov"];

$dia = substr($fecha_ultimo_mov,8,2);
$mes= substr($fecha_ultimo_mov,8,2);
$anio = substr($fecha_ultimo_mov,8,2);

$fecha = $dia."-".$mes."-".$anio;


?>
<body onUnload="window.opener.openedImprimir=0;" onLoad="window.print(); window.close(); cerrar()"> 
<table width="100%" border="1" cellpadding="0" cellspacing="0">
  <tr bordercolor="#FFFFCC" bgcolor="#FFFFFF">
    <td colspan="3"><div align="center"><strong>INVENTARIO PO </strong></div></td>
    <td colspan="2"><div align="center"><font color="#000000" face="Arial, Helvetica, sans-serif">ULTIMA ACTUALIZACION: <?php echo $fecha;?> </font></div></td>
  </tr>
  
  <tr bordercolor="#FFFFFF" bgcolor="#000099">


    <td width="7%" height="23" bgcolor="#999999"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">CODIGO</font></div></td>
    <td width="20%" bgcolor="#999999"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">PRODUCTO</font></div></td>

<td width="25%" bgcolor="#999999"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">PRESENTACION</font></div></td>

<td width="8%" bgcolor="#999999"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">COD </font></div></td>
<td width="26%" bgcolor="#999999"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">DROGA</font></div></td>

	<td width="6%" bgcolor="#FFFF99"><div align="center"><font color="#000000" size="2" face="Trebuchet MS">SALDO</font></div></td>
</tr>
  
  <?php 



$anio_actual = date("y");
$mes_actual = date ("m");

$sql1="select * from tr_existencias group by cod_mercaderia ORDER BY cod_droga, cod_mercaderia";
$result1 = $db->Execute($sql1);
 
  if (!$result1) die("fallo".$db->ErrorMsg());
  while (!$result1->EOF) {


 $nro_factura=strtoupper($result1->fields["nro_factura"]);
  $cod_mercaderia=strtoupper($result1->fields["cod_mercaderia"]);
   $precio_unitario=strtoupper($result1->fields["precio_unitario"]);
    $fecha_ultimo_mov=strtoupper($result1->fields["fecha_ultimo_mov"]);
	 $proveedor=strtoupper($result1->fields["proveedor"]);
	  $cod_interno=strtoupper($result1->fields["cod_interno"]);
 	   $cod_droga=strtoupper($result1->fields["cod_droga"]);







$sql2="select *, sum(cantidad_ingresada) as cantidad_ingresada, sum(cantidad_salida) as cantidad_salida from tr_existencias where cod_mercaderia = '$cod_mercaderia' ";
$result2 = $db->Execute($sql2);


$cantidad_ingresada=strtoupper($result2->fields["cantidad_ingresada"]);
$cantidad_salida=strtoupper($result2->fields["cantidad_salida"]);




//$fecha_ultimo_mov=strtoupper($result1->fields["fecha_ultimo_mov"]);

$dia1 = substr($fecha_ultimo_mov,8,2);
$mes1 = substr($fecha_ultimo_mov,5,2);
$anio1 = substr($fecha_ultimo_mov,0,4);

//$fecha_ultimo_mov = $dia1."-".$mes1."-".$anio1;

$sql="select * from monodrogas where cod_barra = $cod_mercaderia ";
$result = $db->Execute($sql);
$nombre_comercial=strtoupper($result->fields["nombre_comercial"]);
$presentacion=strtoupper($result->fields["presentacion"]);
$troquel=strtoupper($result->fields["troquel"]);

 $sql="select * from drogas where cod_droga = $cod_droga";
$result = $db->Execute($sql);
 $drogas=strtoupper($result->fields["droga"]);


$vto_lote = $mes_lote." - ".$anio_lote;



$cantidad_existente = $cantidad_ingresada - $cantidad_salida;
	
$mes = $mes_lote;
$anio = $anio_lote;

if ($anio == ""){
	$anio = $anio_actual;
}
else
		  {
$estado = "-";
		  }



if (($anio != "00") && ($mes != "00") or ($anio != "00") or ($mes != "00") ){

if ($anio < $anio_actual){
$estado = "VENCIDO";
}
else{

if ($anio > $anio_actual){
$estado = "-";}
else{

if ($mes < $mes_actual){
$estado = "VENCIDO";
}
else{
	$estado ="-";
}
}
}
}

if ($troquel == ""){
$troquel = $cod_mercaderia;
}

/*$sql="select * from stock_inventario where cod_mercaderia = $cod_mercaderia";
$result = $db->Execute($sql);
$cantidad_agrupado=strtoupper($result->fields["cantidad"]);
*/

 $sql="select sum(cantidad) as cantidad from tr_stock where cod_mercaderia = $cod_mercaderia and cod_movimiento between '1' and '3'";
$result = $db->Execute($sql);
 $cantidad_ingresada_stock=$result->fields["cantidad"];

 $sql="select sum(cantidad) as cantidad from tr_stock where cod_mercaderia = $cod_mercaderia and cod_movimiento between '6' and '6'";
$result = $db->Execute($sql);
$cantidad_salida_stock=$result->fields["cantidad"];

 $existencia_stock = $cantidad_ingresada_stock - $cantidad_salida_stock;


$sql3 = "INSERT INTO `oncologico`.`existencias_unido` (`nro_factura`, `cod_detalle`, `cod_mercaderia`, `lote`, `mes_lote`, `anio_lote`, `cantidad_ingresada`, `precio_unitario`, `cantidad_salida`, `fecha_ultimo_mov`, `proveedor`, `cod_interno`) VALUES ('$nro_factura', '', '$cod_mercaderia', '$lote', '$mes_lote', '$anio_lote', '$cantidad_ingresada', '$precio_unitario', '$cantidad_salida', '$fecha_ultimo_mov', '$proveedor', '$cod_interno');";
//mysql_query($sql3);

 
?>

    <tr bgcolor="#FFFFFF"><td><div align="center"><font size="2" face="Trebuchet MS"><?php print("$troquel");?></font></div></td>
    <td><div align="left"><font size="2" face="Trebuchet MS"><?php print("$nombre_comercial");?></font></div></td>
    <td><div align="left"><font size="2" face="Trebuchet MS"><?php print("$presentacion");?></font></div></td>
<td><div align="left"><font size="2" face="Trebuchet MS"><?php print("$cod_droga");?></font></div></td>
	<td><div align="left"><font size="2" face="Trebuchet MS"><?php print("$drogas");?></font></div></td>

<td bgcolor="#FFFF99"><div align="center"><font size="2" face="Trebuchet MS"><?php print("$cantidad_existente");?></font></div></td>
  </tr>
<?php 
 
$result1->MoveNext();
	}
  

?>
</table>
